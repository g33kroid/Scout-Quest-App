// Placeholder — the real leader home screen (the 15-second scoring flow) is
// Task 05. This exists so the auth/route-guard/TOTP chain has somewhere
// real to land once fully authenticated (aal2).
export default function LeaderHomePage() {
  return (
    <main className="mx-auto flex min-h-dvh w-full max-w-sm flex-col justify-center px-6 py-8">
      <p>Signed in.</p>
    </main>
  );
}
